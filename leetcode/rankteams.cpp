#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    int T;
    cin >> T;
    vector<string> votes(T);
    for(string &v : votes) cin >> v;
    ll length = votes[0].length();
    map<ll, vector<ll>(N)> rankings;
    for(string v : votes){
        for(int i = 0; i < length; i++){
        }
    } 
}
