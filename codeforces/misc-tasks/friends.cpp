#include <bits/stdc++.h>
using namespace std;

int main(){
    int T, y, n, d, c, count;
    cin >> T;
    vector<int> o;
    while(T--){
        string q; cin >> q;
        n = 0; y = 1; d = 0; c = 1, count = 0;
        if(q.length() != 1){
            for(char i : q){
               
        }
        o.push_back(c);
    }
    for(int p : o) cout << p << endl;
}
