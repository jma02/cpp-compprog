#include <bits/stdc++.h>

using ll = long long;
using namespace std;

void solve(){
    int n, k; cin >> n >> k;
    vector<int> vec(n);
    for(int &i : vec) cin >> i;
    int x = 0, y = 3001;
    for(int i  = 0; i < n; i++){
        
    }
    cout << (x / k) - (y) << '\n';
}

int main(){
    ios_base::sync_with_stdio(false);  
    cin.tie(NULL);
    int t; cin >> t;
    while(t--){
        solve();
    }
}

